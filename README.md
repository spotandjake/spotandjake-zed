# Spotandjake Zed

This repo contains a custom zed extension used to tune my zed editor setup.

### Features

Currently this extension only provides a simple theme named `spotandjake-dark`. The theme is based off an older version of the theme used on the repl.it website, with some modifications that match personal preferences.
